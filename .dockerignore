# Build artifacts — never ship the host target dir into the build context.
target/
**/target/

# VCS & CI
.git/
.github/

# Local dev / deploy scaffolding not needed inside the image
# (web/ stays IN: fanout embeds web/live.html via include_str! at compile time)
docker-compose.yml
Makefile
deploy/
docs/
*.md

# Editor / OS cruft
.vscode/
.idea/
*.swp
.DS_Store
